<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.neha.explicit_intent.MainActivity">

    <Button
        android:id="@+id/btnStartNewActivity"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:drawableLeft="@drawable/ic_arrow_forward_black_24dp"
        android:text="Start New Year"
        android:textColor="#FFFFFF"
        android:textAllCaps="false"
        android:backgroundTint="@color/colorPrimary"
        android:layout_centerHorizontal="true"/>
    <ImageView
        android:id="@+id/imgPhotu"
        android:layout_width="150dp"
        android:layout_height="150dp"
        android:layout_below="@id/btnStartNewActivity"
        android:src="@drawable/hny"
        android:scaleType="centerCrop"
        android:transitionName="IMAGE_ANIM"/>
</RelativeLayout>
