<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.neha.multithreading.MainActivity"
    tools:background="@color/colorPrimary"
    android:backgroundTint="@android:color/holo_blue_dark">

    <Button
        android:id="@+id/btnStartThread"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Start Thread"
        android:backgroundTint="@color/colorAccent"/>

    <Button
        android:id="@+id/btnFatchedData"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Fatched Data"
        android:backgroundTint="@color/colorAccent"
        android:layout_below="@id/btnStartThread"/>

    <ImageView
        android:id="@+id/imgWallpaper"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/btnFatchedData"
        android:scaleType="centerCrop"
        />
</RelativeLayout>
